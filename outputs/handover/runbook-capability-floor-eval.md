# Runbook: run the capability-floor eval and update the report

**Reader:** AR (re-run quarterly as new models release)
**Status:** stub — blocked on the eval dataset + runner existing (the July gap item);
write this alongside building them, not after

## To cover

- Where the eval dataset lives, its task taxonomy, and how to version it
- Running the eval against the model panel; where results land
- Updating thresholds and the capability-floor report; how the result feeds routing
  and the self-hosting migration triggers
- Adding a new model or a new task class to the panel

## Verification

- [ ] AR has produced one report update without M
